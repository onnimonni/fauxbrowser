# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# build artifacts (binary at repo root only — NOT cmd/fauxbrowser/)
/fauxbrowser
/tls-fp-proxy
/result
*.log
*.pid

# generated / provided CA material — NEVER commit
ca.pem
ca.key
*.crt
!*.example.crt

# WireGuard / VPN configs & secrets
*.conf
.env
.env.*
!.env.example
